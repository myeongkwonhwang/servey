/**
 * 외부 채널 polling.
 * 토스 / 쿠팡 / 네이버 / 유튜브쇼핑 등의 외부 API 를 READ-only 로 폴링해 outbox 에 적재.
 * Inbound Adapter (Polling Publisher with Outbox) 패턴.
 */
package io.github.orange2652.partner.channel.batch.external;
