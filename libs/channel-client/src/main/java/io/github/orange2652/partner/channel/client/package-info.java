/**
 * 외부 채널 통신 클라이언트 라이브러리.
 * Feign Client / Token Provider / Rate Limiter / Error Decoder / Recovery / Strategy 인터페이스.
 * channel-batch (외부 READ) 와 channel-adapter (외부 WRITE) 양쪽이 사용.
 */
package io.github.orange2652.partner.channel.client;
