# 2026-06-10 Worklog

## Summary
- Phase 4 ⑦ 부터 Round B (step 2 FAIL 보상) 까지 — A1 saga 의 step 1 ~ 3 정상 흐름 + step 1 보상 흐름 모두 구현 완료
- 구조/명명 재정의 3건: saga step 재명명 / flow only 토픽 / Gateway 명명 + 별도 lib 분리 (5 → 6 libs)
- 정책 결정 3건: LocalDateTime 시각 정책 / 외부+DB 일관성 / Pivot+보상 의미 정리
- 토스 공식 문서 조회로 step 1 확장 + Round B 보상 흐름 명세 확정
- 문서 — saga-flow.md / module-flow.md 작성 + mermaid 에러 수정

---

## 작업 단위 (시간순)

### ① Phase 4 ⑦ — saga step 1 command/reply 흐름
- saga-orchestrator → channel-adapter cmd → reply 흐름 첫 구현
- 토픽: `saga.order.cmd.staging_insert` / `saga.order.reply.staging_insert` (이후 ⑦ 에서 flow only 로 통합됨)
- JSON payload + Kafka header (`saga-id`, `saga-type`, `saga-step`, `command-type`)
- 기존 단순 consumer 흡수 (TossOrderReceivedConsumer 제거)
- 메모리: [[project-pcm-phase4-step7-saga-step1]]

### ② 누락 테스트 + Javadoc 보강
- 단위 테스트 5종 — `OutboxEventTest`, `PollingCursorTest`, `StagingOrderTest`, `ProcessedEventTest`, `OutboxPublisherTest`
- Port 인터페이스 4종 + 도메인 record 4종 메서드별 Javadoc 보강
- 신규 단위 테스트: `SagaStateAdvancerTest` (3 case)

### ③ Saga 구조 재정의
- 사용자가 4 publish 케이스 정의: orderReception / unconfirmedOrder / confirmedOrder / sendInvoice
- A1 (orderReception → unconfirmedOrder → validate → confirmedOrder) + B1 (sendInvoice) 분리
- validate 별도 step 유지 (Compensatable)
- 토픽 / step / 클래스 명명 컨벤션 명문화 + 기존 `staging_insert` → `unconfirmed_order` 일괄 재명명
- saga-orchestrator 패키지 `saga/step1/` → `saga/unconfirmedOrder/` + `saga/state/` 분리 (SagaStateAdvancer 공통화)
- 메모리: [[project-pcm-saga-redefinition]]

### ④ 시각 타입 정책 변경 — LocalDateTime
- 회사/팀 컨벤션 채택: `Instant` + `TIMESTAMPTZ` → `LocalDateTime` + `TIMESTAMP` (without TZ)
- DB Migration: channel V3 / saga V2 — `TIMESTAMPTZ AT TIME ZONE 'Asia/Seoul' → TIMESTAMP`
- `hibernate.jdbc.time_zone` 제거. jackson.time-zone 유지
- 코드 일괄 변환 (도메인 5 + JPA Entity 5 + Service/Client/DTO + 테스트 5)
- 메모리: [[project-pcm-localdatetime-migration]]

### ⑤ A1 step 2 validate — service-core 합류
- service-core 가 saga 흐름에 처음 참여
- 신규: `Validator` (TOSS PAID 룰) + `ValidateCommandConsumer` + `ValidateReplyPublisher`
- saga-orchestrator: `ValidateCommandPublisher` + `ValidateReplyConsumer`
- `UnconfirmedOrderReplyConsumer` 확장 — OK 후 validate command 발행
- service-core kafka serializer String 으로 통일 (이전 JsonSerializer 였음 — 합의 정책 미적용)
- `ServiceCoreApplication` scan 범위 확장
- 신규 테스트: `ValidatorTest` 5 case
- 메모리: [[project-pcm-step2-validate]]

### ⑥ 토픽 구조 — flow only
- 사용자 결정: step 별 토픽 분리 → flow 단위 통합
- `saga.order.cmd` / `saga.order.reply` 두 토픽으로 통합 (4 토픽 → 2 토픽)
- step 분기는 `command-type` header prefix filter
- Publisher 4 종 토픽 변경 + Consumer 4 종 filter 추가 (`startsWith(PREFIX)`)
- 메모리: [[project-pcm-flow-only-topics]]

### ⑦ A1 step 3 confirmedOrder — Pivot
- service-core 단독 책임 (외부 물류 호출 + 내부 주문 INSERT 같은 saga step)
- lib (channel-client/logistics 신규): `LogisticsClient` Port + `ShipmentRequest/Response` (이후 ⑫ 에서 Gateway 로 변경됨)
- service-core: `MockLogisticsClient`, `TossOrderParser`, `OrderPersister`, `ConfirmedOrderCommandConsumer`, `ConfirmedOrderReplyPublisher`
- lib persistence-common: `Order` 도메인 + Port + JPA Entity/Repository/Adapter
- DB Migration: `core_schema V1` — orders + processed_event 테이블
- `ProcessedEventJpaEntity.@Table` 의 schema 명시 제거 (default_schema 공유)
- saga-orchestrator: `ConfirmedOrderCommandPublisher` / `ReplyConsumer` + `ValidateReplyConsumer` 확장
- 신규 테스트: `OrderTest` (2 case), `TossOrderParserTest` (2 case)
- 메모리: [[project-pcm-step3-confirmed-order]]

### ⑧ A1 step 1 확장 — 토스 공식 문서 확인 후
- 사용자 정의: step 1 = 기본검증 + 상품준비중 API + staging INSERT
- 토스 공식 문서 조회 (`mcp__toss-shopping-docs`):
  - 정상 흐름: `PAID → PREPARING_PRODUCT → DELIVERING → DELIVERED → CONFIRMED_ORDER`
  - `PUT /orders/products/status` — PAID → PREPARING_PRODUCT
  - `POST /seller-cancel` — 상품준비중 상태에서도 취소 가능 ★ (Round B 활용)
- lib (channel-client/toss 추가): `TossOrderStatusClient` Port + `OrderProductStatusChangeResult` + `TossOrderStatuses` 상수
- channel-adapter: `BasicValidator` (fast fail), `MockTossOrderStatusClient`
- `UnconfirmedOrderCommandConsumer` 흐름 확장 — ④ 기본검증 → ⑤ 외부 호출 → ⑥ DB Tx → ⑦ reply
- 신규 테스트: `BasicValidatorTest` 7 case
- 메모리: [[project-pcm-step1-extension]]

### ⑨ 외부 + DB 일관성 정책 결정
- 사용자 질문: "저장과 API 한 step 처리 시 outbox 또 사용?"
- 4 패턴 비교 (A: 외부→DB Tx / B: DB→외부 / C: Outbox 의도 / D: 2PC)
- 본 프로젝트 step 1 = A 채택 — 외부가 멱등성 추정 + saga 보상으로 결손 보완
- Outbox 도입 우선순위 매트릭스: channel-batch (✅) / saga reply 발행 (🟡) / Round B 보상 (🟡) / step 1 PREPARING_PRODUCT (❌)
- 메모리: [[project-pcm-external-db-consistency-decision]]

### ⑩ Pivot / 보상 의미 정리
- 사용자 질문: "각 단계의 보상과 Pivot 의미"
- step 별 Type / 부작용 / 보상 정의 표 + UX/운영 함의
- step 1 = Compensatable, step 2 = Compensatable (read-only), step 3 = Pivot ★
- step 2 가 보상 트리거 역할 (자체 부작용 X — step 1 보상 발행)
- 메모리: [[project-pcm-saga-pivot-compensation-summary]]

### ⑪ LogisticsClient → LogisticsGateway + 별도 lib
- 사용자 의도: 자사 물류 = DB-to-DB interface (HTTP 아님)
- Clean Architecture Gateway 패턴 채택 — protocol 무관 추상화
- 별도 lib 신설: `libs/logistics-gateway/` (5 → **6 libs**)
- 패키지 `io.github.orange2652.partner.channel.gateway.logistics`
- `MockLogisticsClient` → `MockLogisticsGateway`. `ConfirmedOrderCommandConsumer` import / 변수명 변경
- 메모리: [[project-pcm-logistics-gateway-naming]]

### ⑫ saga-orchestrator 별도 app 검토 (구조 유지)
- 사용자 질문: "saga-orchestrator service-core 통합 가능?"
- 별도 app 의 4 가지 근거 보강 (책임 분리 / 변경 빈도 / DB schema lifecycle / Phase 1 분할 준비)
- 통합 시 조건 (cross-service saga 없음 / Phase 1 분할 없음 등) — 본 프로젝트 미충족
- 결정: 별도 유지. 메모리 [[project-pcm-4app-architecture-decision]] 보강

### ⑬ Round B — step 2 FAIL 보상 흐름
- 흐름: `step 2 FAIL → compensate → 외부 seller-cancel → staging UPDATE → ABORTED`
- 사용자 결정 순서: **외부 먼저 → 내부 동기화** (외부가 진실 소스)
- lib (channel-client/toss): `TossOrderCancelClient` + `CancelOrderRequest/Result` (+ `sagaCompensation()` 정적 팩토리)
- lib (channel-event-schema): SagaSteps / SagaCommandTypes 의 `UNCONFIRMED_ORDER_COMPENSATE_*` 추가 + `UnconfirmedOrderCompensateCommand/Reply` records
- lib (persistence-common): `StagingOrder.status` (ACTIVE/CANCELED) + `markCanceled()` + `isCanceled()` + `findByChannelAndExternalOrderProductId` Port
- DB Migration: `channel_schema V4` — staging_order ADD COLUMN status
- saga-orchestrator: `SagaStateAdvancer` 에 `compensate/abort/complete` 추가. `compensate/` 패키지 신규. `ValidateReplyConsumer` FAIL 분기 확장
- channel-adapter: `MockTossOrderCancelClient` + `saga/compensate/` 패키지 (Persister + Consumer + Publisher) 신규
- **prefix 충돌 방지**: 기존 consumer `startsWith(UNCONFIRMED_ORDER)` 가 보상도 매칭 → 정확 매칭 (`UNCONFIRMED_ORDER_REQUEST`) / 더 긴 prefix (`UNCONFIRMED_ORDER_REPLY`) 로 좁힘
- 신규 테스트: `StagingOrderTest` 의 `markCanceled` + null 방어 확장
- 메모리: [[project-pcm-round-b-compensation]]

### ⑭ 문서 작성 — saga-flow.md / module-flow.md
- `docs/diagrams/saga-flow.md` — A1 전체 flowchart + state diagram + B1 (계획) + 구현/계획 요약 표
- `docs/diagrams/module-flow.md` — **신규**: App 의존 / lib 의존 / saga 정상 + 보상 sequence / DB schema / 외부 통신 매트릭스 / Round 진행 요약 (7 섹션)
- 4 app + 6 libs 의 모듈 단위 흐름을 한 페이지에서 파악

### ⑮ mermaid 에러 수정
- 사용자 보고: flowchart 렌더링 에러
- 원인: **flowchart edge label 안 괄호** `()` — mermaid 노드 syntax `nodeId(label)` 와 충돌
- fix: 모든 edge label 큰따옴표 escape (`|"..."|`). 노드 정의도 따옴표 통일
- stateDiagram-v2 의 transition label — 따옴표 escape 미지원 → 콜론/괄호 자체를 제거 (대시 / 한글로 치환)
- ⑥/⑦ 번호 중복 정정 (⑧ ~ ⑰ 순차 재번호)
- Round B 보상 흐름 노드 + 빨간색 classDef 추가

---

## 합의된 정책 (이번 세션 신규)

| 정책 | 결정 |
|---|---|
| 시각 타입 | LocalDateTime + TIMESTAMP (회사/팀 컨벤션, KST 단일 시간대) |
| Saga 구조 | A1 4 step + B1 분리. step 명명 `unconfirmedOrder/validate/confirmedOrder` |
| 토픽 | flow only (`saga.{flow}.cmd/reply` 2 토픽), step 은 `command-type` header filter |
| header filter | 정확 매칭 또는 더 긴 prefix (compensate 와 충돌 방지) |
| 외부+DB 일관성 | step 1 = "외부 먼저 + DB Tx". outbox 는 발행 stuck / 보상 우선 |
| 보상 순서 | 외부 먼저 → 내부 동기화 (외부가 진실 소스) |
| Pivot 위치 | step 3 confirmedOrder (orders INSERT 직후) |
| Gateway vs Client 명명 | HTTP/REST → Client. protocol 모호/DB → Gateway |
| lib 구조 | 4 app + **6 libs** (logistics-gateway 신규) |
| saga-orchestrator 별도 app | 유지 — 책임 분리 / 변경 빈도 / lifecycle / Phase 1 분할 준비 |
| **mermaid 작성** | flowchart edge label / 노드 정의 모두 큰따옴표 escape. stateDiagram 은 콜론/괄호 제거 |

---

## 메모리 갱신
- [[project-pcm-phase4-step7-saga-step1]] — ①
- [[project-pcm-saga-redefinition]] — ③
- [[project-pcm-localdatetime-migration]] — ④
- [[project-pcm-step2-validate]] — ⑤
- [[project-pcm-flow-only-topics]] — ⑥
- [[project-pcm-step3-confirmed-order]] — ⑦
- [[project-pcm-step1-extension]] — ⑧
- [[project-pcm-external-db-consistency-decision]] — ⑨
- [[project-pcm-saga-pivot-compensation-summary]] — ⑩
- [[project-pcm-logistics-gateway-naming]] — ⑪
- [[project-pcm-4app-architecture-decision]] (보강) — ⑫
- [[project-pcm-round-b-compensation]] — ⑬
- MEMORY.md 인덱스 갱신

---

## git
- 본 세션 stage 파일 100+ (lib / services / migration / docs / settings.gradle)
- untracked: `gradle/`, `gradlew*` — 사용자 명시 시
- commit 미실행

---

## 다음 진입점 후보

| 후보 | 근거 메모리 |
|---|---|
| Pivot 직전 외부 cancel 자동 호출 (step 3 외부 OK + 내부 INSERT 실패 시) | [[project-pcm-round-b-compensation]] |
| B2 취소 saga (Pivot 통과 후 사용자 취소) | [[project-pcm-saga-pivot-compensation-summary]] |
| B1 sendInvoice (물류 회신 saga) | [[project-pcm-saga-redefinition]] |
| saga reply 발행 outbox (모든 publisher 의 stuck 해소) | [[project-pcm-external-db-consistency-decision]] |
| 보상 자동 재시도 outbox (Round B 의 cancel) | [[project-pcm-external-db-consistency-decision]] |
| 다채널 지원 (saga state 채널 컬럼 + ChannelStrategy) | [[project-pcm-round-b-compensation]] |
| 실제 Feign 토스 client (Mock 교체) | [[project-pcm-phase4-session2]] |
