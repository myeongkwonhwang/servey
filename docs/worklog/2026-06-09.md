# 2026-06-09 Worklog

## Summary
- Phase 4 첫 코드 — A1 흐름 ① Flyway baseline 부터 ⑥ saga-orchestrator 첫 단계까지 진행
- end-to-end 187 메시지 흐름 실측 검증 (channel-batch → outbox → publisher → kafka → channel-adapter / saga-orchestrator)
- TIMESTAMPTZ + KST 정착, 테스트/도구 양식 합의, worklog 시스템 도입

---

## 작업 단위 (시간순)

### ① Phase 4 — channel_schema Flyway baseline
- V1 단일 파일 (`V1__create_channel_baseline.sql`) — outbox / polling_cursor / staging_order / processed_event
- channel-batch 가 schema owner. channel-adapter 는 `flyway.enabled: false` + JPA `validate`
- 메모리: [[project-pcm-phase4-step1-flyway]]
- 파일: `services/channel-batch/src/main/resources/db/migration/channel/V1__*.sql`

### ② Outbox + PollingCursor 도메인/인프라 (Hexagonal)
- 위치: `libs/persistence-common/{outbox,cursor}/`
- 패턴: Domain (record) + Port (interface) + Infrastructure (JPA Entity + Adapter, package-private)
- 결정:
  - payload: `String` (JSON) — 직렬화 책임 호출자
  - JSONB ↔ String: Hibernate 6 native `@JdbcTypeCode(SqlTypes.JSON)`
  - `libs/persistence-common/build.gradle` 에 `api 'spring-boot-starter-data-jpa'` 추가
  - `ChannelBatchApplication` scan 범위 확장 (`io.github.orange2652.partner.channel`)
- 파일: 11 신규 java + 1 build.gradle + 1 Application

### ③ TossOrderClient + MockTossOrderClient + Scheduler
- 위치:
  - `libs/channel-client/toss/` — `TossOrderClient` (Port) + `TossOrderPage` / `TossOrderItem` record
  - `services/channel-batch/.../external/toss/` — `MockTossOrderClient` (학습용 빈 페이지) + `TossOrderPollingService` + `TossOrderPoller`
- 흐름:
  1. cursor 조회 (Spring Data 자체 Tx)
  2. 외부 API 호출 (Tx 밖)
  3. WRITE Tx — outbox INSERT + cursor 갱신 (한 Tx atomic)
- `TransactionTemplate` 사용 — self-invocation 함정 회피
- 토스 API 분석 (mcp__toss-shopping-docs__searchDocumentation):
  - 식별자: orderProductId (PK 단위), orderId (주문건)
  - 페이지네이션: cursor 기반 + (startDate, endDate) 필수, 최대 31일
  - 상태 enum ~19종

### ④ Outbox Publisher + TopicRouter (Polling Publisher = Message Relay)
- 위치: `services/channel-batch/.../publisher/OutboxPublisher` + `OutboxTopicRouter`
- 동작: `findUnpublished(50)` → `KafkaTemplate.send` (5s blocking ack) → `markPublished`
- `OutboxRepositoryAdapter.markPublished` 에 `@Transactional` 추가
- 토픽 매핑: `OutboxTopicRouter` 의 `Map<aggregateType, topic>` 단순 추상화 — 향후 type 별 publisher 분리 hook
- application.yml: producer/consumer value-(de)serializer 를 모두 `StringSerializer/Deserializer` 로 통일

### TIMESTAMPTZ + KST 전환 (V2)
- 모든 시각 컬럼 `TIMESTAMP` → `TIMESTAMPTZ` (V2 ALTER COLUMN)
- application.yml: `hibernate.jdbc.time_zone: Asia/Seoul` + `spring.jackson.time-zone: Asia/Seoul`
- 코드는 `Instant` 그대로 (변경 0)
- 결과: psql 에서 KST `+09` 표시 + DB 는 UTC + tz 메타 보존

### ⑤ A1 saga step 1 — channel-adapter consumer + staging INSERT
- 위치:
  - `libs/persistence-common/{staging,idempotency}/` (11 신규 java)
  - `services/channel-adapter/.../external/toss/` — `TossOrderPayload`, `StagingOrderPersister`, `TossOrderReceivedConsumer`
- 멱등성: `processed_event` 테이블 — `eventId = "{partition}:{offset}"`
- Tx 경계: `StagingOrderPersister` 별도 `@Component` 분리 (`@Transactional` self-invocation 회피)
- application.yml: KST + StringDeserializer + `auto-offset-reset: earliest`
- `ChannelAdapterApplication` scan 범위 확장
- **검증**: staging_order 187 / processed_event 187 (1:1). UNIQUE 제약 동작 확인

### ⑥ saga-orchestrator 도입 — saga state INSERT 단계
- 위치:
  - `services/saga-orchestrator/.../db/migration/saga/V1__*.sql` (saga_schema baseline)
  - `libs/persistence-common/saga/` (5 신규 — `SagaStatus`, `SagaState`, `SagaStateRepository`, JpaEntity, JpaRepository, Adapter)
  - `services/saga-orchestrator/.../inbox/` — `SagaStarter` + `ChannelOrderReceivedConsumer`
- 멱등성: `(saga_type, correlation_key)` UNIQUE → `DataIntegrityViolationException` catch + skip
- `SagaOrchestratorApplication` scan 확장 + `build.gradle` 에 `spring-boot-starter-json` 추가 (ObjectMapper auto-config)
- **신규 테스트**: `SagaStateTest` (6 case), `SagaStarterTest` (2 case) — JUnit5 + AssertJ + Mockito
- **검증**: saga_state 187 RUNNING. UNIQUE 제약 동작

### 합의된 정책 (이번 세션 신규)
| 정책 | 결정 |
|---|---|
| Migration owner | 같은 schema 공유 서비스 중 primary writer 가 owner |
| 시각 컬럼 | TIMESTAMPTZ + hibernate.jdbc.time_zone + jackson.time-zone = Asia/Seoul |
| Kafka serializer | 전 서비스 String 통일 (consumer 가 직접 ObjectMapper parse) |
| Scan 범위 | 모든 Application 의 `scanBasePackages` / `@EntityScan` / `@EnableJpaRepositories` = `io.github.orange2652.partner.channel` |
| Tx 경계 | 외부 호출 Tx 밖 + DB 쓰기만 한 Tx. `TransactionTemplate` 또는 별도 `@Component` |
| Saga 시작 멱등성 | UNIQUE 제약 catch (별도 `processed_event` 안 둠 — 학습 단순화) |
| 테스트 양식 | JUnit5 + AssertJ + Mockito. 단위 테스트 우선. 통합 (Testcontainers) 은 별도 라운드 |
| 이전 누락 테스트 | 다음 라운드들에 한 단위씩 끼워넣기 |
| git stage | 매 작업마다. commit 은 명시 시 |
| **worklog** | **매 작업마다 `docs/worklog/YYYY-MM-DD.md` 에 append** |

### worklog 시스템 도입
- `docs/worklog/YYYY-MM-DD.md` 일자별 파일
- 매 작업 단위 끝날 때마다 그날 파일에 append
- 메모리: [[feedback-worklog-per-task]]

---

## 검증 (end-to-end 실측)

```
channel-batch (8081)
  TossOrderPoller @Scheduled(30s) → outbox INSERT
  OutboxPublisher @Scheduled(5s) → Kafka 발행 + markPublished
        ↓ (187 메시지 누적)
  Kafka topic: channel.order.received
        ↓
channel-adapter (8082) — group=channel-adapter, earliest
  TossOrderReceivedConsumer → processed_event check → staging_order INSERT (atomic Tx)
        → staging_order 187 / processed_event 187 (1:1)
saga-orchestrator (8085) — group=saga-orchestrator, earliest
  ChannelOrderReceivedConsumer → SagaStarter (Tx + UNIQUE catch)
        → saga_state 187 RUNNING
```

모든 시각 KST `+09`. payload 안 외부 raw 의 시각은 UTC `Z` 보존.

---

## 메모리 갱신
- [[project-pcm-phase4-step1-flyway]] — ① 결과
- [[project-pcm-phase4-session2]] — ② ~ ⑥ 통합 정리
- MEMORY.md 인덱스 갱신

## git
- 40+ 작업 파일 staged (Java + SQL + yml + 작업 build.gradle)
- untracked: 인프라성 (`docs/`, `gradle/`, `gradlew*`) — 사용자 명시 시
- `commit` 미실행 (사용자 명시 시)

## 종료 상태
- channel-adapter / saga-orchestrator bootRun 종료
- Docker compose 계속 (postgres / kafka / kafka-ui)

## 다음 진입점 후보
- ⑦ saga step 1 command 발행 (saga-orchestrator → channel-adapter command/reply)
- 이전 누락 테스트 보완 (한 단위씩)
- 실제 Feign 토스 client (Mock 교체)
- 운영 보강 (ShedLock / readOnly Tx / graceful shutdown / Adaptive backoff)
- `idempotency_keys` 테이블 (saga step 실행 라운드와 같이)
