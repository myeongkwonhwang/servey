# 2026-06-08 Worklog

## Summary
- 정체성 합의 → 4 app 아키텍처 결정 → SAGA 결정 (Q1/Q2/Q3/Q4) → A1 흐름 saga 정의 → Phase 0 인프라 (Gradle/의존성/Docker/application.yml) 완료
- 산출물: 코드 골격 28 files + 인프라 설정 + 6 ADR/메모리

---

## 작업 단위

### 1. 프로젝트 정체성 + 4 app 아키텍처 결정
- 결정: 멀티채널 (쿠팡 / 네이버 / 유튜브쇼핑 / 토스쇼핑) 제휴 주문/클레임 흐름을 **MSA + 분산 SAGA** 기반으로 학습
- 4 Spring Boot app + 5 libs (Multi-gradle)
  - `channel-batch` / `channel-adapter` / `service-core (ordr+claim 통합)` / `saga-orchestrator`
- ordr/claim 경계 모호로 service-core 한 service 안 모듈 보존, Phase 1 분리 가능
- 메모리: [[project-pcm-4app-architecture-decision]] (5 app → 4 app 변경)

### 2. SAGA 결정 (ADR-0001)
| Q | 결정 |
|---|---|
| Q1 (스타일) | **오케 일관**. 후행 흐름 추가 시 하이브리드 전환 검토 → [[project-pcm-q1-orchestration-decision]] |
| Q2 (구현) | **자체 구현**. Phase 1+ 에서 Eventuate/Temporal 비교 학습 → [[project-pcm-q2-saga-implementation]] |
| Q3 | (메모리 미존재 — Q3 항목 확인 필요) |
| Q4 (보상 멱등성) | **Idempotency-Key 테이블 + Outbox dedup 둘 다** → [[project-pcm-q4-compensation-idempotency]] |

### 3. A1 흐름 SAGA 정의
- 4 step + Pivot at step 4
  - step 1 비정규 적재 (channel-adapter) — Compensatable
  - step 2 판매가능조건 검증 (service-core) — read-only
  - step 3 외부 confirm API (channel-adapter) — Compensatable
  - step 4 내부 주문 적재 (service-core) — **Pivot**
- saga 시작 트리거: channel-batch 가 외부 폴링 → outbox(received) → Kafka → saga-orchestrator
- 메모리: [[project-pcm-a1-saga-definition]], [[project-pcm-a1-flow-decisions]], [[project-pcm-asis-a1-flow-correction]]

### 4. A1 흐름 합의 결정
| 항목 | 결정 |
|---|---|
| Message Relay | Polling Publisher (CDC 아님, 학습 부담 ↓) |
| Consumer 멱등성 | `processed_event` 테이블 |
| Schema 포맷 | JSON 시작 → 안정화 후 Avro/Schema Registry 검토 |
| DLQ | 처음부터 도입 |
| DB Schema 분리 | **옵션 A** — 같은 DB / schema 분리 (`channel_schema` / `core_schema` / `saga_schema`) |
| Outbox 위치 | Producer (channel-batch) 사유 schema 안 |

### 5. Phase 0 인프라 (1 ~ 3 단계)

#### Phase 1 — Gradle Wrapper + 첫 빌드
- Gradle 9.5.1 (Java 26 호환 — 8.x 는 major version 70 비호환)
- Java toolchain 21
- `./gradlew assemble` — 29 tasks 모두 성공

#### Phase 2 — 의존성 추가
| 영역 | 라이브러리 |
|---|---|
| Kafka | `spring-kafka` |
| ORM / 영속성 | `spring-boot-starter-data-jpa` |
| DB Driver | `postgresql` (runtimeOnly) |
| Migration | `flyway-core` + `flyway-database-postgresql` |
| 외부 API | `spring-cloud-starter-openfeign` (channel-batch, channel-adapter 만) |
| Validation | `spring-boot-starter-validation` |
| 운영 | `spring-boot-starter-actuator` |
- Spring Cloud BOM `2024.0.1`

#### Phase 3 — 로컬 환경 + application.yml
- `docker-compose.yml`: PostgreSQL 16 + Kafka 7.7.1 KRaft + Kafka UI (port 8090)
- 각 서비스 application.yml — schema 옵션 A 적용, JPA `ddl-auto: validate`, Kafka JSON serializer
- Server port: 8081 / 8082 / 8083 / 8085

### 6. 코드 골격 28 files
- services × 4 + libs × 5 + root 설정
- 각 service Application + 각 lib package-info

---

## 메모리 / ADR
- [[project-pcm-phase0-infrastructure]] — 인프라 완료 결과
- `docs/adr/0001-*.md`, `docs/adr/0002-*.md`
- `docs/as-is/toss-shopping.md`, `docs/transition.md`

## 종료 상태
- 사용자가 직접 git init + first push
- 다음 진입점: Phase 4 (첫 코드) — A1 channel-batch Scheduler + Flyway migration
