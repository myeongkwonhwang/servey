# 2026-06-11 Worklog

## Summary
- 리팩터링 라운드 1 — 누적 중복 / 매직 스트링 / 안티패턴 9 항목 일괄 정리
- 도메인별 unchecked 예외 계층 신설 (PartnerChannelException + Saga/Channel sub + final 구체)
- Publisher 4개 통합 (1개 베이스), Channel enum 도입, Optional.get() 무방어 제거, Kafka 헤더 / 멱등성 가드 유틸 신설
- depth defense + 어휘 분리 (Command vs *_REQUEST) 명문화 — 통합 대신 Javadoc
- end-of-day skill 신설 — 메모리 + worklog 마무리 절차

---

## 작업 단위 (시간순)

### ① 리팩터링 후보 스캔
- Explore agent — services / libs 123 Java 파일 대상
- 후보 20건 보고 → 직접 코드 확인으로 유효 9건 압축
- 검증 사실: `BizRuntimeException` 클래스 실재 없음 (CLAUDE.md 규칙이었음), `log.info` 만 사용 컨벤션은 잘 지켜져 있음

### ② 예외 정책 합의
- 옵션 비교 — sealed base + 도메인 sub vs 도메인별 독립 sealed vs 평면
- 위치 비교 — 각 도메인 lib/service vs common-domain vs 하이브리드
- 사용자 선택: sealed base + 각 도메인 lib/service 분산
- 충돌 발견 — `module-info.java` 없음 (unnamed module) → sealed 는 같은 패키지 강제
- 충돌 해소: `abstract` base + 도메인별 분산 (sealed 포기, 컨벤션 + abstract 로 외부 확장 차단)

### ③ T3 예외 인프라 신설
- 신규 7 파일:
  - `libs/common-domain/.../common/exception/PartnerChannelException.java` (abstract base, `code` 필드)
  - `services/saga-orchestrator/.../saga/exception/` SagaException (abstract sub) + SagaPublishException + SagaStateException (final)
  - `services/channel-adapter/.../adapter/exception/` ChannelException (abstract sub) + UnsupportedChannelException + ChannelPayloadException (final)
- 베이스 시그니처: `(code, message)` + `(code, message, cause)`. code 는 구체에서 hardcode
- 컴파일 검증 통과

### ④ T4 Publisher 4 → 1 통합
- saga-orchestrator 의 `*CommandPublisher` 4 개 obsolete 삭제 (UnconfirmedOrder / Validate / ConfirmedOrder / UnconfirmedOrderCompensate)
- 신규 `SagaCommandPublisher` (saga.publisher 패키지) — `publish(sagaId, sagaStep, commandType, command)` 단일 진입점
- 예외 정책: 직렬화 / Kafka send 실패 / timeout / interrupt → `SagaPublishException` throw
- 호출자 3 파일 try-catch 패턴으로 변경 — `ChannelOrderReceivedConsumer`, `UnconfirmedOrderReplyConsumer`, `ValidateReplyConsumer` (2 호출처)
- 신규 단위 테스트 3 case — `SagaCommandPublisherTest` (정상 발행 + 헤더 4 검증, 직렬화 실패, Kafka 실패)

### ⑤ T5 Channel enum 도입
- 신규 `libs/common-domain/.../common/Channel.java` — 현재 TOSS 1 값. `fromCode(String)` (Optional), `code()` (= name())
- 적용 범위: 비교 / 상수만 enum 화. record 의 `String channel` 필드는 유지 (점진 — 별도 라운드)
- 10 main 파일 치환: 4 channel-adapter (UnconfirmedOrderCommandConsumer 의 inner `UnsupportedChannelException` 도 우리 도메인 예외로 교체), 3 saga-orchestrator, 1 channel-batch, 2 service-core
- 매직 스트링 5 가지 다른 이름 (`CHANNEL`, `CHANNEL_TOSS`, ... ) → `Channel.TOSS.code()` 통일

### ⑥ T6 Optional.get() 무방어 정리
- 6건 모두 isEmpty() guard 직후의 `.get()` — 안전하지만 코드 스멜
- 전부 `.orElseThrow()` 단순 치환 — 의미 변화 없음, 인덴트 유지
- 대상: SagaStateAdvancer (2), UnconfirmedOrderReplyConsumer, ValidateReplyConsumer (2), StagingOrderCancelPersister

### ⑦ T7 KafkaHeaderExtractor 신설
- 위치: `libs/kafka-infra/.../kafka/header/KafkaHeaderExtractor.java`
- 메서드: `stringOrEmpty(byte[])` (null → ""), `uuid(byte[])` (null/invalid → Optional.empty)
- 적용 5 consumer — `byte[] → String / UUID` 변환 + try-catch + null 가드 보일러플레이트 제거
- 호출자 패턴: `KafkaHeaderExtractor.uuid(h).orElse(null)` + null 체크
- 신규 단위 테스트 5 case — `KafkaHeaderExtractorTest`

### ⑧ T8 IdempotencyGuard 신설
- 위치: `libs/persistence-common/.../persistence/idempotency/IdempotencyGuard.java`
- AOP 안 씀 — 명시적 호출 wrapper (Spring Component)
- 정적 메서드 `eventId(partition, offset)` 빌더 + `isAlreadyProcessed(consumerName, eventId)` 체크
- 적용 3 consumer — `ProcessedEventRepository` 직접 사용 → `IdempotencyGuard` 위임. processed_event INSERT 는 호출자 Tx 안에서 그대로 (책임 분리)

### ⑨ T9 Command/Request 명명 명문화
- 옵션: 명문화 vs 헤더값 *_COMMAND 통일 vs 클래스명 *Request
- 사용자 선택: 명문화
- `SagaCommandTypes` Javadoc 보강 — Command (도메인 record / DDD) vs *_REQUEST/*_REPLY (Kafka 통신 어휘) 의 의도적 분리 명시
- 코드 변경 없음

### ⑩ T10 Validator depth defense 명문화
- BasicValidator (channel-adapter, step 1) ↔ Validator (service-core, step 2) 둘 다 `PAID` 체크
- 옵션: 명문화 vs step 1 만 유지 vs step 2 만 유지
- 사용자 선택: 명문화 (depth defense)
- 두 Javadoc 상호 참조 + 책임 차이 명시:
  - step 1 = "외부 호출 가능성" fast fail (PUT /orders/products/status 전)
  - step 2 = "판매 가능 비즈니스 룰" (재고 / 가격 / 채널 등록 등으로 미래 확장)
- 코드 변경 없음

### ⑪ T11 테스트 픽스처 정리
- testFixtures gradle plugin 도입 안 함 (학습 단순화)
- 5 테스트 파일에서 `"TOSS"` 리터럴 / `private static final String CHANNEL = "TOSS"` → `Channel.TOSS.code()` 사용
- 대상: BasicValidatorTest, ValidatorTest, StagingOrderTest, PollingCursorTest, OrderTest
- 전체 테스트 통과

### ⑫ T12 CLAUDE.md + memory 갱신
- CLAUDE.md (gitignore — 개인 작업 지침):
  - Item 69/70 예외: `BizRuntimeException` → **도메인별 unchecked 계층** (PartnerChannelException + abstract sub + final 구체)
  - Item 55 Optional: **`Optional.get()` 무방어 호출 금지** — `orElseThrow()` / `orElse(null)`
  - 매직 스트링 항목: 채널 식별자는 `Channel` enum (common-domain) 추가
- memory: `project_pcm_refactoring_round_1.md` 신규 + MEMORY.md 인덱스 한 줄 추가
- 협업 스타일 feedback memory 2 신규:
  - `feedback_exception_policy.md` — 도메인별 분리 예외 선호
  - `feedback_clarify_over_consolidate.md` — 표면 중복 → 명문화 우선

### ⑬ end-of-day skill 작성
- 위치: `.claude/skills/end-of-day/SKILL.md`
- 4 단계: 오늘 한 일 식별 → 메모리 저장 → worklog 작성 → 보고
- worklog 정책 강제: 워딩 / 메모리 참조 제외, 작업 / 결정 사실만. 이미 오늘 자 있으면 append. 작업 단위 ①②③ 환원숫자

---

## 합의된 정책 (이번 세션 신규)

| 정책 | 결정 |
|---|---|
| 도메인 예외 계층 | abstract base (`PartnerChannelException`, common-domain) + 도메인별 abstract sub + final 구체. sealed 는 unnamed module 에서 분산 불가 → 포기 |
| 예외 위치 | 각 도메인 lib / service 안 (공통 베이스만 common-domain) |
| Optional 사용 | `Optional.get()` 무방어 호출 금지. `.orElseThrow()` / `.orElse(null)` 명시적 처리 |
| 채널 식별 | `Channel` enum (common-domain). 비교 / 상수는 `Channel.TOSS.code()`. record 필드는 점진 |
| 멱등성 가드 | AOP 안 씀. 명시적 wrapper component (`IdempotencyGuard`) + `eventId(partition, offset)` 빌더 |
| 명명 충돌 | Command (도메인 / DDD) vs *_REQUEST (Kafka 어휘) — 어휘 분리 의도적, 통일 안 함 |
| 표면 중복 | 검증 레이어 / 책임이 다르면 통합 X. Javadoc 으로 depth defense 명문화 |
| Test 픽스처 공유 | testFixtures plugin 도입 안 함. enum 값으로 매직 스트링만 제거 |

---

## git
- stage 35 파일 (+619 / -437)
- 추가 12 / 수정 18 / 삭제 4 + SagaCommandTypes Javadoc 1
- untracked 유지: `gradle/`, `gradlew`, `gradlew.bat` (사용자 unstage 선택 — 별도 commit)
- CLAUDE.md, `.claude/skills/` — gitignore (개인 작업 지침)
- commit 미실행

---

## 다음 진입점 후보

| 후보 | 설명 |
|---|---|
| record `channel: String` → `Channel` | 본 라운드 미루어짐. event-schema record 6종 + 호출자 + Jackson 직렬화 확인 |
| `OutboxTopicRouter` 예외 치환 | `IllegalStateException` → messaging / channel-batch 도메인 예외. T7 / kafka-infra 라운드와 함께 가능 |
| Pivot 직전 외부 cancel 자동 호출 | step 3 외부 OK + 내부 INSERT 실패 시 |
| B2 취소 saga | Pivot 통과 후 취소 요청 흐름 |
| B1 sendInvoice | 물류 회신 saga |
| saga reply 발행 outbox | 모든 publisher 의 stuck 해소 |
| 보상 자동 재시도 outbox | Round B 의 cancel 보장 |
| 다채널 지원 | PollingStrategy 추상화 + ChannelStrategy |
